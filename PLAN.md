# CANScope 二期规划(Phase 2)

> 一期(v1.0.0)已交付:Web → 桌面迁移完成、9 套冒烟 130+ 断言全过、PyInstaller 免安装包上线。
> 原迁移计划(现状盘点/技术选型/里程碑 M1-M6/发布后修复记录)已完成使命,随二期规划发布而移除,
> 修复记录完整保留于 [CHANGELOG.md](CHANGELOG.md)。
>
> **更名说明**:项目由「CAN 分析仪(CANAnalyzer / blf-dbc-desktop)」更名为 **CANScope**
> (CAN 总线分析仪;exe:CANScope.exe,仓库目录:canscope),详见 CHANGELOG Unreleased。

---

## 一、定位与二期总原则

**产品实质**:BLF × DBC 的 CAN/CAN FD **离线总线分析与回放工具**,软件示波器形态。
二期的每一项都应围绕三条主线展开:

| 主线 | 一句话 |
|---|---|
| A 分析深挖 | 从"能看曲线"到"能出结论"(区间统计/对比/导航) |
| B 数据互通 | 进得起更多日志格式,出得去可复现的工件 |
| C 工程化 | CI 自动打包,摆脱手工交付 |

硬性原则(继承一期):
- `core/` 零 UI 依赖分层不动摇;新数据源一律实现 `FrameSource` 接口;
- 性能预算与一期对齐:**20 万点绘制流畅、GB 级日志解析不卡 UI**(449MB/1393 万帧 67s 为基准线);
- 每个里程碑可运行、有验收标准,冒烟测试同步扩充。

---

## 二、里程碑

### R1 图表与分析补强 —— "从能看曲线到能下结论" ✅(2026-08-27 完成)

> 实现与验收记录:五项功能全部落地;新增 `tests/smoke_r1.py` 24 项断言
> (真实事件路径),全套 8 套冒烟回归通过。开发期修复关键坑:
> `QGraphicsView.mapToScene 仅接受 QPoint`(QPointF 在事件派发栈内引发原生段错误)。

| # | 功能 | 说明 |
|---|---|---|
| 1.1 | y 轴量程控制 | 每窗独立:双击自动适应 ↔ 手动锁定(min/max 输入);避免多信号混画时自动量程跳变 |
| 1.2 | 区间框选统计 | 左键拖矩形(RubberBand):框内各信号 min/max/mean/std/Δt 浮层显示,一键「应用到统计页」(复用 signal_stats start/end) |
| 1.3 | 缩略导航条 | 底部 minimap:全时长概览 + 当前视窗框,拖动跳转(解决长记录下"缩放开头在哪"的定位痛点) |
| 1.4 | 曲线临时显隐 | 图例点击切换可见(数据不重解码),对满窗 64 信号时的辨读是刚需 |
| 1.5 | 示波器截图导出 | 右键菜单「导出本窗图像 PNG」(含标题/图例/时间范围水印) |

**验收**:真实 449MB 日志完成全部交互无卡顿;`smoke_gui` 图表项 22 → 30+;
所有新交互走真实事件路径(一期教训④)。

### R2 数据互通与导出扩展 —— "进更多格式,出可用工件" ✅(2026-08-28 完成)

> 实现与验收记录:ASC 往返帧数/解码一致(±1)、MF4 适配 asammdf 8.1.2 实测通过、
> 片段回读帧数/时序一致、Trace 全量导出与页面过滤语义一致、骨架 cantools 可加载;
> 新增 `tests/smoke_r2.py` 21 项断言。开发期关键确认:
> ① BLFWriter 以首个消息为新时间基准(片段相对时序保持、绝对起点归零,已按此定契约);
> ② cantools 扩展帧走 bit31 标志位约定(非 'x' 后缀);
> ③ asammdf 8.x 字节数组以 uint8 二维通道存储字节无损('S' 字符串行存在 NUL 截断语义)。

| # | 功能 | 说明 |
|---|---|---|
| 2.1 | ASC 输入 | Vector/can-tools 兼容 ASCII log(`FileSource` 抽象扩展,detect_kind 增加特征识别) |
| 2.2 | MF4/MDF4 输入 | asammdf 作为可选依赖(缺失仅提示,懒加载),统一转换到帧索引结构 |
| 2.3 | BLF 片段导出 | 按当前缩放区间裁剪出独立 BLF(给 CANoe 复现问题片段,分享场景刚需) |
| 2.4 | Trace 全量导出 | 当前过滤条件下所有分页拉平为 CSV(取消 200 行/页的限制感) |
| 2.5 | DBC 骨架生成器 | 从日志观测 ID+DLC 反推 `BO_ xx Unknown_xxx: dlc ECU` 骨架(未知报文分析的起步器),树中灰 ID 可右键生成 |

**验收**:同一录制源的 BLF vs ASC 解析帧数一致(±1 边界帧);导出片段 CANoe 能打开;
ASC/MF4 样例纳入 smoke_sync 门控矩阵。

### R3 性能与稳定性

| # | 功能 | 说明 |
|---|---|---|
| 3.1 | 崩溃留痕 | sys.excepthook → logs/crash.log(版本+栈+最近操作)、「关于」页显示版本号与构建时间 |
| 3.2 | 依赖升级验证 | Python 3.10+/3.11 与 PySide6 新版回归矩阵(一期基于 3.9 锁定) |
| 3.3 | 性能基准与优化 | 先用真实日志定位瓶颈;仅在多信号解码确有瓶颈时引入 worker 池,仅在大索引内存超预算时引入磁盘溢出缓存 |
| 3.4 | 大索引与统计优化 | >200 万帧时评估分段/mmap;Bus Load 仅在实测二次遍历成为瓶颈时改增量计算 |

**验收**:
- 性能:1393 万帧真实日志解析、解码、绘制和回放达到现有基准,优化前后有可记录的耗时/内存对比;
- 稳定性:人为注入异常必有 `crash.log`,后台任务异常不导致 GUI 退出;
- 兼容性:Python 3.10/3.11 与目标 PySide6 版本的核心冒烟和 GUI 冒烟通过。

### R4 发布体系

| # | 功能 | 说明 |
|---|---|---|
| 4.1 | GitHub Actions CI | windows-latest:冒烟套件(offscreen)→ PyInstaller 打包 → tag 触发 Release 上传产物 |
| 4.2 | 版本单点定义 | VERSION 在一处定义(main_window/构建脚本/关于页共用;构建脚本注入 spec) |
| 4.3 | 用户手册 | 截图型使用文档(操作说明表扩成图文)+ 快捷键速查页(Help 弹窗) |
| 4.4 | 打包产物验证 | CI 启动 `CANScope.exe --smoke`,检查退出码、日志、截图及必需的 `data/` 文件,失败则不发布 |

**验收**:打 tag 出正式包,README 徽章挂 CI 状态;EXE 冒烟和产物完整性检查通过;全程无人工桌面操作。

---

## 三、风险与对策

| 风险 | 对策 |
|---|---|
| asammdf/MF4 引入重依赖 | 可选依赖懒加载;核心分发不含,菜单缺依赖时引导 pip 安装 |
| 框选统计在千万级帧上的响应 | bisect 区间切片复用帧缓存;主线程只拿聚合结果(照搬 fetch_* 后台模式) |
| 更名导致的用户偏好丢失 | 已在 Unreleased 实现 INI 整文件迁移;CI 中加一条旧目录构造→启动→断言偏好在位的用例 |
| 二期摊子过大 | R3/R4 继续按"先过冒烟再谈下一步"推进;性能优化必须以真实数据基准触发,不做预防性重构 |

---

## 四、明确不做(二期范围外)

- macOS/Linux 实测(代码层面保持跨平台不添阻塞,但不承诺、不进验收);
- 服务端/WebSocket/局域网多人形态(离线桌面定位不变);
- UDS/诊断协议栈(另立项目范畴);
- 树莓派部署(延续一期决定)。
