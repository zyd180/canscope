# 更新日志(Changelog)

所有显著变更记录于此。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [Unreleased]

## [2.0.0] - 2026-08-29

### Added(R2 数据互通与导出扩展)
- **ASC 日志输入**:Vector/can-tools 兼容 ASCII 日志按内容识别(含 .log/.txt 改名文件),
  与 BLF 共用同一条索引/统计/解码/回放链路;实测 BLF↔ASC 往返帧数/解码值一致
- **MF4/MDF4 输入(可选依赖 asammdf≥8.0)**:解析 CAN_DataFrame 总线日志组
  (经典 CAN/CAN FD),未安装时给出安装指引;asammdf 8.1.2 实测往返通过
- **BLF 片段导出**:按当前缩放区间裁剪独立 BLF(帧缓存索引 + 堆合并恢复全局时序,
  内存 O(报文数));片段时间轴从 0 起算、相对时序与原日志一致
- **Trace 全量导出 CSV**:当前报文在相同信号值过滤/时间区间下的全部帧,
  列与 Trace 表一致(UTF-8 BOM)
- **DBC 骨架生成器**:报文树新增「未识别报文」灰组(日志有数据但 DBC 未定义的
  ID+DLC),右键一键生成无信号定义的起步 DBC(扩展帧按 bit31 标志位约定,
  cantools/主流工具均可加载);统计链路同步新增按通道 ID+DLC 清单
- 测试:新增 `tests/smoke_r2.py`(21 项断言);MF4 段按 asammdf 安装状态门控

### Added(R1 图表与分析补强)
- **Y 轴量程控制(每窗独立)**:双击绘图区在 自动适应 ↔ 手动锁定 间切换;
  右键「锁定 Y 轴…」可输入精确 min/max;锁定值跨信号刷新持久,标题栏显示 `[Y锁定 lo~hi]`
- **区间框选统计**:Ctrl+左键拖拽框选时间区间,生成条带高亮 + 统计浮层
  (数值信号 min/max/mean/std/计数;值表信号直接展示状态分布);
  「应用本窗统计区间 → 统计页」把区间转为统计页时间过滤(带「清除区间」按钮);
  进入回放时自动清理浮层与预览
- **缩略导航条(minimap)**:示波器列底部全时长概览(每信号 ≤600 点轮廓线,
  颜色同曲线),当前视窗高亮:中部拖拽平移、两侧边缘拖拽改跨度、滚轮缩放,
  与主图缩放共用钳制链路并双向同步
- **曲线临时显隐**:点击窗口内信号图名(色点+名称,手型光标)切换显隐,
  不重新解码、跨刷新保持;隐藏态样式置灰;右键「显示全部曲线」一键还原
- **示波器图像导出**:右键「导出本窗图像 PNG…」,整窗截图并附加
  `CANScope · 视窗/量程模式/信号数` 水印行
- 测试:新增 `tests/smoke_r1.py`(24 项断言,全部走真实事件路径),
  全套回归 8 套冒烟通过

### Changed
- **项目更名**:「CAN 分析仪」(CANAnalyzer / blf-dbc-desktop)→ **CANScope**(CAN 总线分析仪):
  - exe 由 `CANAnalyzer.exe` 更名 `CANScope.exe`;调试控制台变体同步为 `CANScopeDbg`
  - 应用标识变更:`ApplicationName=CANScope`、`OrganizationName=canscope`(仓库目录更名 canscope)
- QSettings INI 路径随组织名变化,**首次启动自动迁移旧偏好**
  (`config/blf-dbc-desktop/CAN Analyzer.ini → config/canscope/CANScope.ini`),
  抖动标记开关 / 选中信号三态记忆无损保留
- 打包脚本 build.ps1、PyInstaller spec、窗口标题 / 工具栏品牌区、测试用例中的应用标识全部同步

## [1.0.0] - 2026-08-25

首个正式发布版本。由 Web 版(`blf-dbc-web`)完整迁移为 PySide6 桌面应用:
**零浏览器、零部署、双击即用**。

### Added(新增)

#### 解析核心
- BLF/CAN FD 流式单遍扫描:帧索引 + 统计一次完成,支持进度回调
- 帧 LRU 缓存(4 文件 / 200 万帧,mtime+size 校验)
- DBC 加载:UTF-8/GBK/GB18030 编码主动检测
- 文件类型按内容识别(BLF 魔数 "LOGG" / DBC 文本特征),支持 .log/.txt 改名文件
- 信号解码 + 均匀降采样(20 万点);帧源抽象 + 堆合并回放引擎

#### 界面与功能
- 报文/信号树:通道分组、ECU 彩色标签、无数据灰色删除线、实时搜索、可向左折叠收起
- 多示波器:纵向堆叠(≤64 信号 / 20 色调色板)、值表信号自动阶梯线、
  chip 单窗管理、每窗独立关闭(自动重编号)、空窗自动复用、右键全中文菜单
- 图表交互:滚轮缩放(鼠标为中心,最小窗 0.5s,边界钳制)、Shift+滚轮滚动页面、
  拖拽平移、跨窗光标读数 + 同步竖线、Shift+单击锚点测量、抖动峰值标记
- 已选信号侧栏:锚点值(红)/ 光标值(蓝)双列常驻显示
- CANoe 式回放:0.5~10x 变速、进度拖拽 seek、x 轴固定生长式绘制、诊断行
- Trace 报文流:分页 200/页、信号值搜索(数值容差/状态名)、按当前缩放区间过滤
- 统计:ID 分布条形图 + Bus Load 负载率、信号 min/max/mean/std/超范围警示、
  周期/抖动峰峰/丢帧估计
- CSV 导出(UTF-8 BOM,Excel 直开)
- 配置弹窗:总线类型/波特率(影响 Bus Load)、通道 DBC 重映射、抖动峰值标记
- 选中信号三态记忆(首次自动选/有记录恢复/清空保持)
- 大文件解析进度条(按文件读取位置,0.5% 节流)
- 深色主题 + **Windows 深色标题栏/边框**(DWM 匹配主题色,Win10 19041+ / Win11)
- **启动默认最大化窗口**
- 深色主题(配色自 Web 版完整移植)、演示数据一键生成

#### 打包与交付
- PyInstaller onedir 免安装目录 + 应用图标
- `--smoke` 冒烟自检(自动开演示数据 → 校验 → 截图 → 退出码)
- 测试:9 套冒烟共 130+ 项断言(含 449MB/1393 万帧真实大文件可选验证)

### 性能
- 449MB / 1393 万帧真实日志:解析 67s(进度平滑),缓存解码毫秒级,
  20 万点降采样绘制流畅,回放 10x 无掉帧

### Fixed(开发期内修复的关键问题,详见 PLAN.md)
- QSplitter 作为中央部件时 maximumSize 被 Qt 钳到 minimumSizeHint,
  底部大片留白 → 显式最小高度 + resizeEvent 动态跟随窗口
- 示波器曲线图形泄漏(删除信号后曲线残留)→ 元组解包修复 + 场景级回归断言
- 「+信号」1 基/0 基窗口序号错位,导致复制错窗/凭空新建示波器
- 树点击信号无反应(`QTreeWidgetItem.isEnabled` 在 PySide6 不存在,
  槽内 AttributeError 被 Qt 静默吞掉)
- 通道映射保存不生效(`Signal(dict)` 携带 int 键静默丢槽,容器信号一律改 `object`)
- 小按钮文字被全局 QSS 内边距裁剪(逐一补 `padding:0`)
- QSettings 注册表后端对含盘符键写读不一致 → 改用 INI 文件后端(便携式)
- 浮点累加导致测试数据多写一轮 → 整数循环;GBK 控制台乱码 → UTF-8 输出

### Removed(随迁移一并移除的 Web 概念)
- FastAPI/uvicorn 服务端、WebSocket、HTTP 上传(改为文件对话框)
- 树莓派/systemd 部署、分块上传、vendor 遗留库(uPlot/lightweight-charts)
